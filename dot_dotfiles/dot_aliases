# alias

# map ls to eza with a flag or two
alias ls='eza -la --header --icons --git --no-user --no-permissions --git-repos --group-directories-first --time-style "+%Y-%m-%d %H:%M"'

# pnpm is hard to type…
alias pn='pnpm'

# Open current dir in finder:
alias finder='open -a Finder ./'
